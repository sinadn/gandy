package com.example.gandy.util;

// TODO write response codes and descriptions in a file and read in cache while service starts. then write a static function to receive description from error code
public class ResponseCodeUtils {

    public static String getDescription(String code) {
        return "";
    }

    
}
